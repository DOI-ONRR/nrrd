---
applications:
- name: hasura-dev-interface
  buildpacks:
   - staticfile_buildpack
  memory: 100M
  services:
   - nrrd-b-psql

  # this is a manifest to build the interface to the database.  It is used for automated tunneling. 
