/* eslint-disable max-len */
// https://material-ui.com/customization/globals/
const {
  primary,
  secondary,
  grey,
  common,
  ...rest
} = require('./palette')

module.exports = Object.freeze({})
